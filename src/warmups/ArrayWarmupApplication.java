package warmups;


import util.Input;

public class ArrayWarmupApplication {
    public static void main(String[] args) {
//        WarmupExercises vacationSpots = new WarmupExercises();
//
//        vacationSpots.stringToArray("Testing to see if this works");

        //07/15/21 - Warmup
        // //WARM UP
        // TODO Given this string: `String str = "Oberon is the
        //  best cohort ever!"`, print out the character from that
        //  string using a random number generator, that generates
        //  a number between 0 and 50. Using a try/catch block, handle
        //  any errors (Exceptions) that may occur by printing out the
        //  message.
        //**Method comes from WarmupExercises.java

//        WarmupExercises warmupExercise = new WarmupExercises();
//
//        warmupExercise.printRandomCharacter("Oberon is the best cohort ever");


//        Input test = new Input();
//
//        test.getInt("test");

        //Write a method named getTwentyPrimes() that returns a string containing the first 20 prime numbers, each separated by a comma. Output: "1, 2, 3, 5, 7, 11, 13, 17, 19"... until we have a total count of 20 primes in the string.
        //**METHOD CALLED FROM WARMUPEXERCSISES.JAVA
        WarmupExercises warmup071621 = new WarmupExercises();

        warmup071621.getTwentyPrimes();





    }
}